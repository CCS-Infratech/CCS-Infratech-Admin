import ProjectForm from '@/features/projects/components/product-form';

const page = () => {
  return (
    <div className='h-[calc(100vh-64px)]'>
      <ProjectForm pageTitle='Project Editor' />
    </div>
  );
};

export default page;
